# v1.0.0.1.45.0 Desktop Visual Check

1. Launch Invio from a clean desktop state. No small blank/white `Invio` window should flash while the main window opens.
2. Navigate away from Providers, then enter Providers repeatedly. No separate blank/white `Invio` window should appear.
3. Search/filter Providers; no separate native window should appear during reflow.
4. Confirm Provider logo remains 40px and Available/Verified appears directly under Provider Name, not below the logo.
5. Confirm cards are visibly shorter/compact, descriptions still ellipsize, version remains bottom-right, and Install/Uninstall behavior is unchanged.
