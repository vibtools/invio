# Global Forms + Settings UI Canary — v1.0.0.1.42.0

1. Open New Customer List, Add Account, Invoice Template and New Task; confirm 32px controls, 6px radius, 12px dialog padding, 8px spacing and blue primary action.
2. Open Settings, press `Ctrl+F`, search `logs`, `folder`, `window`, and `customer`; confirm only matching cards remain and clearing search restores all cards.
3. Resize the application; Settings uses two columns when space permits and one column when narrow.
4. Select Reset Settings; confirm values change in the UI but persistence occurs only after Save Changes.
5. Confirm existing provider/account/task/settings behavior is unchanged.
