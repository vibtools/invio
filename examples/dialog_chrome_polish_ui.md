# v1.0.0.1.48.0 Dialog Chrome Visual Canary

Apply the delta over the exact v1.0.0.1.47.0 baseline and verify on Windows:

- Main title-bar Close control has a small right inset.
- App-owned dialog Close control has the same compact right inset.
- Add Account, New Customer List, Re-test Account, Invoice Template and New Task show the dialog name only once in the custom title bar.
- Dialog border/shadow is subtle and separates the modal from the dimmed parent without glow.
- Drag, resize, Close, Esc/Enter/Tab, API Test, Save/Create/Cancel and modal behavior remain unchanged.
