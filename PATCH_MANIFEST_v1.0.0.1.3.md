# Invio v1.0.0.1.3 Delta Patch Manifest

Baseline: `v1.0.0.1.2`
Target: `v1.0.0.1.3`
Patch format: project-root replace-ready ZIP with no wrapper directory.

Approved scope:

1. Invoice Template compact UI, provider-supported reusable invoice fields, uppercase currency catalog, and template/provider/task execution binding.
2. Settings typography/spacing and visible checkbox checkmark.
3. Compact Live Logs and Reports redesign using supplied Vib Tools references while preserving Invio actions.
4. Dashboard page using actual Invio state.
5. Directly required tests, version metadata, README/CHANGELOG/public docs and private forensic records.

The patch does not add customer/billing/shipping/payment fields, change provider manifests, replace WorkerManager, alter account exclusivity, or add dependencies.
