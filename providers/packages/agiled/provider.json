{
  "id": "agiled",
  "name": "Agiled",
  "version": "1.0.0",
  "description": "Agiled provider package with Bearer API-key verification against the current Public API; Task sending remains fail-closed because the current OpenAPI does not publish an invoice email/send operation or field-level invoice mutation schema.",
  "credential_fields": [
    {
      "key": "api_key",
      "label": "API Key",
      "kind": "password",
      "required": true,
      "placeholder": "Agiled API key"
    }
  ],
  "account_modes": [
    "Default"
  ],
  "capabilities": [
    "invoice",
    "send_invoice",
    "api_test"
  ]
}
