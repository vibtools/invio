# Invio v1.0.0.1.28

## P10 Verification Correction

`v1.0.0.1.28` re-audits the exact `v1.0.0.1.27` P10 release and corrects one durable uncertainty-reconciliation root cause.

- Mutating `Started`/`Uncertain` evidence is tracked across the full Task delivery history.
- A later success resolves prior ambiguity only for the exact same stage and exact same non-empty deterministic idempotency key.
- Later unrelated deterministic failures cannot overwrite unresolved historical ambiguity as a definitive Failed recipient.
- Durable history now fail-closes on historical recipient/provider/primary-account/assigned-account drift.
- SQLite remains schema v5 with exactly three P10 tables.
- Task.id remains the Stripe idempotency basis; run_id remains audit identity only.
- WorkerManager, Task state machine, P08 retry policy, P09 scheduling, Stripe business flow, dependencies, provider manifests, UI pages, Refrens P11 gate and Agiled fail-close are unchanged.

Production progress remains **10/14**. Next phase: **P11 - Refrens End-to-End Task Enablement**, subject to separate approval.
