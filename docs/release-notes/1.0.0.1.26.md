# Invio v1.0.0.1.26

## P09 CI / Repository-Contract Verification Correction

GitHub Actions run `31336019074` / job `93301866645` reproduced a deterministic CI failure in `test_p09_completion_records_are_synchronized`. The test directly opened `project/planning/PHASE_COMPLETION_LOG.md`, `project/planning/PRODUCTION_ROADMAP.md`, and `project/README.md`. The repository intentionally ignores `/project/`, so those files exist in the full private baseline ZIP but not in a clean public GitHub checkout.

### Corrected

- mandatory P09 completion assertions now use tracked public `README.md`, `ROADMAP.md`, and `docs/release-notes/1.0.0.1.25.md`;
- private `project/` completion records remain additionally verified when a full private baseline is present;
- release metadata is synchronized to `v1.0.0.1.26`;
- the P09 runtime implementation remains unchanged except normal release/User-Agent markers.

### Unchanged

- deterministic `recipient_ordinal_round_robin_v1` primary assignment;
- Stripe 20 requests/second/account, burst 1;
- runtime-only cooldown/health and deterministic eligible fallback;
- no cross-account replay after provider execution begins;
- P08 retry/shutdown behavior;
- one Task = one QThread;
- SQLite schema v4 and dependencies;
- Refrens P11 gate, Agiled fail-close, plugin architecture and all P10+ behavior.

Production progress remains **9/14**. Next: **P10 - Persistent Delivery Ledger, Idempotency and Recovery**, subject to separate approval.
