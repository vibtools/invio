# Invio v1.0.0.1.33 - P13 Forensic Verification Correction

**Status:** P13 COMPLETE / verification-corrected. P11 remains **IMPLEMENTED / LIVE ACCEPTANCE PENDING**.

## Corrected

- Adapter metadata validation after `create_adapter()` is now inside the same fail-closed containment boundary as import/entrypoint execution. `BaseException` from metadata access/conversion is reported as an incompatible external adapter and cannot terminate Invio startup.
- External-provider uninstall now stages the active manifest and adapter registry names. If moving the adapter fails after the manifest was moved, the manifest is restored before the uninstall error is surfaced, preventing half-uninstalled provider state.
- Added focused regressions reproducing both v1.0.0.1.32 defects.

## Preserved

- `EXTERNAL_ADAPTER_INTERFACE_VERSION = 1` and the existing `provider.json + adapter.py` bundle contract.
- Existing P06 preflight, P08 retry classification, P09 optional pacing and P10 write-ahead/restart behavior.
- Packaged Stripe/Refrens behavior, Agiled fail-close, historical `register_task_runner()` compatibility and one Task = one QThread.
- SQLite schema v5 with exactly the existing three P10 delivery-ledger tables.
- Dependencies, Settings, Reports/Logs and page inventory.
- P11 live Refrens acceptance remains pending and is not represented as passed.

## Verification

Final release acceptance requires the updated regression suite, repository audit, clean-public checkout simulation and fresh v1.0.0.1.32 + delta overlay to pass on the final sealed bytes.
