# Invio v1.0.0.1.18

P06 forensic verification/correction release.

Corrected:

- built-in packaged-manifest self-validation against executable runtime truth;
- immutable Task Account-input binding validation;
- Refrens static currency validation using Invio's existing approved catalogue;
- exact Refrens endpoint trust, including rejection of explicit ports;
- Providers-page declared/effective capability reporting for pre-existing registry conflicts;
- prior documentation that overclaimed Invio's fixed Stripe currency catalogue as a universal provider list.

SQLite remains schema v4. P05 immutable execution inputs, WorkerManager, provider-send semantics, dependencies, packaged provider manifests and P07+ behavior are unchanged. Production progress remains 6/14; P07 is not implemented.
