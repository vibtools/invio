# Roadmap

The next approved milestone is backend completion against the frozen 0.1.0 UI: provider API verification, secure persistence, provider adapters, real worker execution, retry/idempotency hardening, and integration tests. No UI redesign is implied by this roadmap.
